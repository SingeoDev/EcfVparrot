
{{ form_start(form, {'attr': {'id': 'contactForm'}})}}

<div class="container-fluid text-center border border-3 border-black contactColor">
    
    {# {{ form_row(form.aboutvehicle, {'attr': {'style': 'display:none;'}, 'label_attr': {'style':'display:none;'}}) }} #}
    {# {{ form_row(form.aboutvehicle, {'label_attr': {'class': 'hidden'}}) }} #}

    <br>
    {{ form_row(form.firstName, {'label_attr': {'class': 'me-2'}}) }}
    <br>
    {{ form_row(form.lastName, {'label_attr': {'class': 'me-2'}}) }}
    <br>
    {{ form_row(form.email, {'label_attr': {'class': 'me-2'}}) }}
    <br>
    {{ form_row(form.phone, {'label_attr': {'class': 'me-2'}}) }}
    <br>
    {{ form_row(form.message, {'label_attr': {'class': 'me-2'}}) }}
    <br>

    <div class="form-check mt-4">
        <input type="checkbox" class="form-check-input" id="terms" name="terms" required>
        <label class="form-check-label" for="terms">
            Accepter la politique de confidentialité. 
            <a class="knowMore" href="{{path('app_privacy_policy')}}">En savoir plus</a>
        </label>
    </div>
    <br>
   
</div>
    <br>
    
    <div class="text-center">

    <button type="reset" class="text-center btn btnCancel border border-3 border-black">Annuler</button>

    <button type="submit" class="btn fs-3 border border-3 border-black submitContact" style="color: #002BC6;">ENVOYER</button> 

    </div>

{{ form_end(form) }}